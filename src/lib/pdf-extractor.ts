// TODO: PDF field extraction using pdf-parse + regex
// extractInvoiceData(buffer: Buffer): Promise<ExtractedInvoice>
// Fields: clientName, clientEmail, invoiceNumber, amount, dueDate
// NO AI — pure pdf-parse + regex only
