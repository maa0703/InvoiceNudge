// TODO: Hardcoded reminder email templates with variable injection
// reminder1(vars), reminder2(vars), reminder3(vars), reminder4(vars), checkin(vars)
// Variables: clientName, invoiceNumber, amount, dueDate, cancelUrl
// NO AI — pure string templates only
